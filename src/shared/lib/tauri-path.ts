import { appDataDir, appConfigDir, appLocalDataDir, appLogDir, tempDir } from "@tauri-apps/api/path";

export const tauriPath = {
  appDataDir,
  appConfigDir,
  appLocalDataDir,
  appLogDir,
  tempDir,
};

